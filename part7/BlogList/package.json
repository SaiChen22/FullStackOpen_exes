{
  "scripts": {
    "dev": "concurrently -n \"server,client\" -c \"blue,green\" \"pnpm --prefix server dev\" \"pnpm --prefix client dev\"",
    "build": "pnpm --prefix client build",
    "start": "NODE_ENV=production pnpm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}